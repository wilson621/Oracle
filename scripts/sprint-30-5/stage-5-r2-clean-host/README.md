# Stage 5 R2 clean-host engineering preparation

This qualification-barred revision restores the established Oracle clean-host
model. `Founder-QA-01` receives and runs a future independently verified
transfer directly; it never receives a source repository and does not require
Git, Node, npm, Supabase CLI or Docker.

The precompiled loopback fixture has one narrow purpose: create a deterministic
confirmed-user session so the unchanged installed R6 package can render its
protected Stage 5 routes. It is not a Supabase emulator and makes no identity,
authentication, RLS, persistence or isolation claim. Those claims remain bound
exclusively to accepted Stage 4 R4 evidence.

This directory permits zero transfers, authorities and attempts. Future
execution requires a separate Founder-authorised execution overlay and fresh
transfer identities.
