# Sprint 30.5 Stage 5 R2 Clean-Host Validation Report

**Status:** Lifecycle correction passed; immutable R6 contrast defect requires Founder decision
**Classification:** Non-qualification engineering validation
**Authority created:** No
**Attempt created:** No
**Transfer created:** No
**Last reviewed:** 4 August 2026

## Passed validation

- exact R6 MSIX and accepted R6/R12/R4 evidence-index hashes reverified;
- qualification-barred zero-transfer/zero-authority/zero-attempt contract;
- ten adversarial contract mutations rejected;
- clean-host fixture compiled on the engineering workstation and hash-bound;
- loopback health, exact Supabase browser CORS preflight, confirmed session,
  credential rejection and unsupported-endpoint rejection passed;
- Windows PowerShell/Edge semantic probe passed eight routes without Node or a
  repository;
- all PowerShell sources parsed;
- runtime-source scan found no Git, Node, npm, Supabase CLI, Docker,
  `C:\Dev\project-meta` or repository-root dependency;
- accepted GPU observer, Companion fixture, activation, runtime-configuration
  and race-tolerant teardown policies were isolated into R2 and manifest-bound;
- preparation manifest contains fifteen exact file hashes; and
- both failed R1 transfers remain prohibited immutable history.

The semantic self-test found two defects before acceptance: query-bearing
navigation was incorrectly compared with `location.pathname`, and the bounded
self-test page's default link colour failed contrast. Both were corrected and
the complete eight-route self-test then passed.

## Preserved failure investigation

Restart Manager identified the `journal.baj` locker as the exact-profile Edge
Network Service child, proving a launcher/child teardown race. The corrected
profile lifecycle was validated on both success and primary-failure paths:
`Browser.close` was requested, no ownership-verified profile process remained,
the exact fresh profile was removed, and the primary error was preserved
separately from cleanup. Historical failed profiles and diagnostics were not
modified or reused.

Main-only caught-exception validation then progressed through these distinct
fail-closed layers:

1. real browser authentication initially failed at fetch because
   `X-Supabase-Api-Version` was absent from the fixture CORS allow-list;
2. after that correction, protected routing exposed a missing source-harness
   web-session secret already supplied by the installed R2 runtime contract;
3. after source-harness alignment, all protected rendering prerequisites passed
   and rendered contrast measurement identified enabled R6 text at 4.22–4.25:1.

The first two were harness/protocol defects and are corrected with regression
coverage. The third is an exact R6 product defect against the frozen 4.5:1
threshold. Candidate-tree inspection confirms the same `text-slate-500` source
in immutable candidate `ee8fbeb7a8d18d393cc9a3e92d622250eb2165ff`.

## Blocked next step

No further installed rehearsal is justified because it would deterministically
fail the frozen contrast gate. No product source or MSIX was changed. A Founder
must authorise a product accessibility correction and accept the consequent new
Stage 2-led requalification chain before engineering can continue. No transfer,
authority, attempt or qualification state was created.
