# Sprint 30.5 Stage 4 R1 — Live Authentication and Protected Rendering Plan

**Status:** Prepared and validated; execution requires a separate Founder decision
**Operating model:** OEOM v1.0
**Execution surface:** Frozen Web source from accepted Stage 2 Requalification R3 candidate `a7fc67f207d9c95407c70812828fa66bd487285d`, tree `356f6d52f1bf70065692e892af8bf916acc8727a`
**Stage 2 / Stage 3:** R3 and R9 are Founder-accepted, closed and immutable. Stage 5 is unauthorised.

## Objective and authority

Qualify canonical Email + Password authentication against a disposable local Supabase topology and prove protected rendering, API authority and cross-account isolation without changing accepted product source or contacting production.

Preparation cannot mint authority or evidence. A separate Founder decision must bind the reviewed preparation commit/tree, a fresh pre-authority record/hash, one unique Founder grant and the exact single-execution token. Only the governed executor creates matching authority/attempt IDs. Locations are create-only; no retry, replacement or phase bypass exists.

## Architecture and trust boundaries

Supabase Auth owns credentials and verification. The client receives only the anonymous key; service-role material is server-only and prohibited from evidence. Protected server/API boundaries independently repeat verified Account checks. Account and Operator remain distinct, and two synthetic Accounts prove isolation.

The disposable topology is PostgreSQL, GoTrue, Kong, PostgREST and Mailpit using contract-bound image digests, `example.invalid` identities, local mail capture and contract ports on a host admitted with no active IPv4 or IPv6 default route. Production endpoints, data and delivery are prohibited. Stage 4 executes frozen Web source: the accepted MSIX is rehashed but not installed, so certificate admission is not applicable.

## Governed lifecycle

1. Consume exact Founder authority after fresh pre-authority admission.
2. Verify repository, candidate and immutable Stage 2/3 bindings.
3. Admit the disposable provider and network-isolated route.
4. Build the exact frozen Web source with attempt-scoped configuration.
5. Prove anonymous rejection; create two unverified Accounts and prove no session.
6. Capture local confirmation, verify, authenticate and render protected content.
7. Prove protected API authority and cross-Account isolation.
8. Sign out and prove unauthenticated enforcement resumes.
9. Remove identities, sessions, messages, database volume, containers, network, configuration and build output.
10. Prove zero residue; freeze, archive, hash and atomically reconcile evidence.

Every required journey must have exactly one affirmative record. Two distinct principals and Operators, one own binding each, zero cross-account leaks, no retained secret/production resource and complete teardown are mandatory. Failure stops forward execution and permits only bounded teardown. Pass stops at Founder evidence review and does not authorise Stage 5.

## R3 reconciliation and protection

The recovered draft blocker was resolved by bounded Migration 011/012 corrections and accepted Stage 2 R3. Exact corrected migration and R3 evidence hashes are contract-bound. Product drift, stale preflight, identity/hash mismatch, reparse traversal, unapproved tool/image, non-loopback publication, invalid journey, leak, process failure, residue or evidence inconsistency fails closed.

Stage 2 R1/R2/R3 and Stage 3 evidence roots are deny-listed and rehashed, never rewritten. Stage 4 does not install/re-sign MSIX, create trust, deploy production persistence, publish, distribute, release, or begin Stage 5.
