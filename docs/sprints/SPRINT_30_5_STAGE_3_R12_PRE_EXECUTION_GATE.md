# Sprint 30.5 Stage 3 Requalification R12 Pre-Execution Gate

**Status:** First package closed; corrected replacement preparation active
**Replacement transfer construction:** Founder-authorised
**Execution:** Not authorised
**Qualification attempt:** Not authorised

## Current boundary

The first R12 package is an immutable pre-authority engineering failure. Its
transfer, custody record, failed `Founder-QA-01` continuity record and expired
identity set must not be modified or reused. The Founder authorised correction,
validation and create-only construction of one independently verified
replacement transfer on the exact approved medium.

This authority stops at verified replacement-transfer preparation. It creates
no grant, authority, attempt, continuity record or qualification evidence.
`package.json` continues to expose validation and rehearsal only; governed
transfer construction uses the token-gated script directly from its approved
source. Qualification execution remains barred.

## Governed bindings

The replacement must bind `ExpectedHarnessCommit`,
`ExpectedTransferManifestSha256` and `ExpectedTransferCustodySha256`. The exact
Founder-bound manifest is the complete
payload inventory authority, the contract-defined mandatory subset must be
present, every manifested byte must be rehashed, and the physical payload
directory must equal the manifest. No expired or historical identity may be
reused.

The immutable product binding remains candidate
`ee8fbeb7a8d18d393cc9a3e92d622250eb2165ff` and MSIX
`492101857733a4cca913243ac660cfb9e181ea796180f1bc9f09c259fe172430`.
The certificate margin closes at `2026-09-01T17:11:50Z`.

The approved physical medium is hardware serial `5F10110403558`, NTFS label
`ORACLE-S3R1`, and volume serial `783A-2CD4`. The replacement R12 output must be
a new create-only sibling and all historical transfers remain immutable.

Stage 4, production, publication and deployment remain not authorised.
