# Sprint 30.5 Stage 2 Requalification R4 Pre-Execution Gate

Status: **HISTORICAL GATE — CONSUMED BY ACCEPTED PASSING ATTEMPT**

## Required repository state

- branch `sprint-9-overlay`
- candidate commit `f7203f9b602b182a2bd006bc3cff3113b839be8e`
- candidate tree `5d7eca4c012874df0b839533dfab283b54778661`
- HEAD is the published R4 preparation commit and descends from the candidate
- candidate and harness product paths are byte-identical
- working tree and index are clean
- exact locked Node/npm, PowerShell, .NET, bsdtar, Electron, WinApp CLI, Next, esbuild and TypeScript versions pass

## Pre-authority admission

Before consuming a Founder grant the wrapper must prove:

1. the exact branch, candidate, tree, harness ancestry, package identity/version and machine identity;
2. all exact R3, Stage 3 R9 and Stage 4 R1 historical bindings and protected roots;
3. zero matching Oracle package and R4 certificate entries;
4. no prohibited ambient `.env*` files or populated runtime credential variables;
5. exact regular-file, non-reparse npm/npx CLI resolution;
6. create-only authority and attempt destinations;
7. all required packaging, signing, verification and teardown tools.

A local developer `.env.local` must be moved to a private location outside the repository before the gate. It must not be copied, logged, hashed into evidence, or restored until the governed attempt and repository-cleanliness verification are complete.

## Execution authority

Preparation supplies no execution authority. The Founder must separately authorise one exact R4 attempt. No retry is implicit. Stage 3, clean-host installation, installed authentication, Stage 5 and release remain unauthorised.
