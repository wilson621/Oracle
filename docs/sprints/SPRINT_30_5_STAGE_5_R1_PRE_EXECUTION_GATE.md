# Sprint 30.5 Stage 5 R1 Pre-Execution Gate

**Status:** Blocked by immutable transfer failure; corrected baseline awaits replacement authority
**Current authority:** Engineering correction complete; replacement transfer not authorised
**Transfer, authority and attempt counts:** One / zero / zero
**Last Reviewed:** 4 August 2026

The qualification-barred R1 preparation baseline remains immutable. The
separately authorised execution-enabled overlay is now prepared and validated.
Before any authority may be created, all of the following must pass in the
current bounded mission.

## Baseline and transfer

1. The accepted R6 MSIX and R6/R12/R4 indexes rehash exactly.
2. The closed R1 preparation commit, tree, manifest and validation records
   rehash exactly.
3. An execution-enabled overlay explicitly records Founder-authorised Stage 5
   qualification.
4. A new create-only transfer uses a fresh identity and exact complete
   inventory.
5. Independent verification proves manifest, custody and payload parity.
6. No prior transfer or evidence namespace is reused.

## Host and zero state

1. Host identity is exactly admitted Founder-QA-01.
2. The permanent founder-provenance exception remains valid and visible.
3. Display is at least 1440 by 900 at exactly 100 or 125 percent scaling.
4. Accepted Intel/NVIDIA hardware adapters and current driver identities are
   recorded; Microsoft Basic Render Driver is rejected.
5. The network state matches the separately authorised Stage 5 provider and
   isolation contract.
6. Oracle package, process, certificate, provider and runtime-configuration
   residue is zero.
7. No production endpoint, credential or data is present.

## Measurement readiness

1. Windows GPU Engine counters return package-PID-addressable samples.
2. Package ownership can be verified from the immutable install location.
3. Windows UI Automation can observe the installed Oracle window and enabled
   focusable controls.
4. Route timing, response size, Guidance timing, semantic tree, contrast,
   reflow, reduced-motion and focus instrumentation is available.
5. The controller can retain one-second samples for two 90-minute cycles
   without dropping below 99-percent completeness.
6. Every unavailable measurement fails before authority.

## Authority creation

Only after every gate above passes may one fresh Stage 5 authority be created
and consumed for one attempt. The attempt must contain both independent cycles.
A consumed authority or permanently failed attempt cannot be retried. No
Stage 6, production, publication, deployment or release authority follows from
Stage 5.
